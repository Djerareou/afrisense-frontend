# API Configuration (Development)
# Backend API base URL (Express on port 3000 with /api prefix)
VITE_API_BASE_URL=http://localhost:3000/api

# Socket.io base URL (same host/port as API)
# Note: Socket.io client connects over HTTP(S) and manages /socket.io internally.
VITE_WS_URL=http://localhost:3000

# Production values (uncomment and adjust when deploying)
# VITE_API_BASE_URL=https://yourdomain.com/api
# VITE_WS_URL=https://yourdomain.com
